// Terminal palette. Amber is the accent, matching the browser harness so the
// two front ends read as one product.

export const ACCENT = '#e0a33e'
export const MUTED = 'gray'

// Markers borrowed from Claude Code's vocabulary, which this UI follows: a
// filled dot introduces something that arrived, an elbow introduces detail
// belonging to the line above it.
export const MARKER = {
  incoming: '⏺',
  self: '>',
  detail: '⎿',
  error: '✗',
  welcome: '✻',
  selected: '❯'
}
